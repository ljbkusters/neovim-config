require("config.settings")	-- vim settings (like tabstop, linenum, etc)
require("config.commands")	-- user commands
require("config.remap")		  -- default vim functionality key remaps (not plugin remaps!)
require("config.lazy")		  -- plugin manager, plugins managed under config/plugins/*.lua
